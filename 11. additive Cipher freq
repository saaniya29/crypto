#include<stdio.h>
#include<string.h>
int main(){
char s[100];
int k,i;
printf("Enter cipher text: ");
gets(s);
for(k=0;k<26;k++){
printf("\nKey %d: ",k);
for(i=0;s[i];i++)
printf("%c",((s[i]-65-k+26)%26)+65);
}
}
