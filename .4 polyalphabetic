#include<stdio.h>
#include<string.h>
int main(){
char p[100],k[100];
int i;
printf("Enter text: ");
gets(p);
printf("Enter key: ");
gets(k);
for(i=0;p[i];i++)
if(p[i]>='A'&&p[i]<='Z')
p[i]=(p[i]-65+k[i%strlen(k)]-65)%26+65;
printf("Cipher Text: %s",p);
}
