#include<stdio.h>
#include<string.h>
int main(){
char s[100];
int b,i;
printf("Enter cipher text: ");
gets(s);
for(b=0;b<26;b++){
printf("\nKey %d: ",b);
for(i=0;s[i];i++)
if(s[i]>='A'&&s[i]<='Z') printf("%c",((s[i]-65-b+26)%26)+65);
}
}
