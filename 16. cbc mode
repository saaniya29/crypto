#include<stdio.h>
#include<string.h>
int main(){
char s[100];
printf("Enter text: ");
gets(s);
for(int i=0;s[i];i++) s[i]+=2;
printf("Encrypted: %s\n",s);
for(int i=0;s[i];i++) s[i]-=2;
printf("Decrypted: %s",s);
}
